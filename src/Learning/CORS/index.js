/*
==> Understand it carefully and apply it: https://anushadasari.medium.com/a-complete-guide-to-cors-9045069a7c5e
==> CORS:
    ->  Cross-Origin-Resource-Sharing
    ->  CORS is a unique web technology in that it has both a 'server-side' and a 
        'client-side' component. The server-side component configures which types of 
        cross-origin requests are allowed, while the client-side component controls how 
        cross-origin requests are made.
*/
/*
    const url = 'http://localhost:3000/client.html' // scheme://host:port/client.html
    const origin = http://localhost:3000/ or scheme://host:port/
    
    Note:  
        A request is a same-origin request when the client origin and the server origin 
        are exactly the same. Otherwise the request is a cross-origin request.
*/
