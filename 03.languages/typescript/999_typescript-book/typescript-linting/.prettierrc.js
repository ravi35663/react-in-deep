{
  // Enforces single quotes instead of double quotes
  // Keeps strings consistent and cleaner in JavaScript/TypeScript
  "singleQuote": true,

  // Ensures semicolons are added at the end of statements
  // Avoids edge cases caused by JavaScript's automatic semicolon insertion
  "semi": true,

  // Adds trailing commas wherever possible (objects, arrays, function params)
  // Makes diffs cleaner and simplifies adding/removing items
  "trailingComma": "all"
}
// ESLint handles logic, Prettier handles formatting.